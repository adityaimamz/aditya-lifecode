import Paticles from "react-tsparticles";
import particleConfig from "./config/particles-config";

export const ParticleBackground = () => {
  return 
  <Paticles params={particleConfig}>
    
  </Paticles>;
};
